name: Test Perfomance With K6

on:
  push:
    branches: ['*']
  pull_request:
    branches: ['*']

jobs:
  backend-compose:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Set up Docker
        uses: docker/setup-buildx-action@v3

      - name: Start backend + services
        run: |
          cd FloginFE_BE/backend
          docker compose up -d

      - name: Wait for MySQL
        run: |
          cd FloginFE_BE/backend
          for i in {1..60}; do
            if docker compose exec -T mysql mysqladmin ping -h localhost -u root -proot &>/dev/null; then
              echo "MySQL is ready!"
              break
            fi
            echo "Waiting for MySQL... ($i/60)"
            sleep 2
          done

      - name: Wait for backend
        run: |
          cd FloginFE_BE/backend
          echo "Waiting for backend to be ready..."
          for i in {1..30}; do
            if docker compose exec -T backend sh -c "wget -q --spider http://localhost:8080/actuator/health"; then
              echo "✅ Backend is ready!"
              exit 0
            fi
            echo "⏳ Waiting for backend... ($i/30)"
            sleep 2
          done
          echo "❌ Backend did not start in time!"
          docker compose logs backend
          exit 1
      
